# Application Configuration
APP_PORT=9000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-me
JWT_EXPIRATION_TIME=3600s # 1 hour, format: https://github.com/vercel/ms

# Database Configuration (PostgreSQL)
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_USER=crmadmin
DATABASE_PASSWORD=crmpassword
DATABASE_NAME=crmdb

# Frontend API URL (if frontend needs to know where the backend is, though served together)
# VITE_API_BASE_URL=http://localhost:9000/api
